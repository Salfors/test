function Fedora() {
	
	case $TODO in 
		"1")
			function Fedora_snap() {

				sudo dnf install snapd 
				sudo ln -s /var/lib/snapd/snap /snap 

			} 
			Fedora_snap ;;

		"2")
			function Fedora_Flatpak(){

				flapak_note= echo -e "\n${SM} your distribution might be out of date and need an upgrade ${EM}\n"

			}
			Fedora_Flatpak ;;

		"3")
			Fedora_snap
			Fedora_Flatpak ;;
	esac
}