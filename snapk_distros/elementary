function elementary() {
	case $TODO in 
		"1")
			sudo apt update 
			sudo apt install snapd ;;

		"2")
			flapak_note= echo -e "\n${SM} your distribution might be out of date and need an upgrade ${EM}\n" ;;

		"3")
			flapak_note= echo -e "\n${SM} your distribution might be out of date and need an upgrade ${EM}\n" 
			sudo apt update 
			sudo apt install snapd ;;
	esac
}